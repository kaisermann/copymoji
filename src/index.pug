extends layouts/base.pug

block content
  ul#recently
  ul#main
    each emoji in self.emojis
      li
        button.copy-btn #{emoji}
  a.goto-recently(href="#recently") recently
  a.goto-main(href="#main") list
